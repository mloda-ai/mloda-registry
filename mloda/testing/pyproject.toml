# AUTO-GENERATED by scripts/generate_pyproject.py
# Do not edit directly - modify config/shared.toml or config/packages.toml instead

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mloda-testing"
version = "0.2.12"
description = "Test utilities for mloda plugin development"
license = "Apache-2.0"
authors = [{ name = "Tom Kaltofen", email = "info@mloda.ai" }]
dependencies = ["mloda>=0.6.1", "pytest>=9.0.3"]
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=9.0.3"]

[project.urls]
Homepage = "https://mloda.ai"
Repository = "https://github.com/mloda-ai/mloda-registry"

[tool.setuptools]
package-dir = {"" = "../.."}
packages = ["mloda.testing", "mloda.testing.data_creator", "mloda.testing.feature_groups", "mloda.testing.feature_groups.data_operations", "mloda.testing.feature_groups.data_operations.aggregation", "mloda.testing.feature_groups.data_operations.mixins", "mloda.testing.feature_groups.data_operations.row_preserving", "mloda.testing.feature_groups.data_operations.row_preserving.binning", "mloda.testing.feature_groups.data_operations.row_preserving.datetime", "mloda.testing.feature_groups.data_operations.row_preserving.frame_aggregate", "mloda.testing.feature_groups.data_operations.row_preserving.offset", "mloda.testing.feature_groups.data_operations.row_preserving.percentile", "mloda.testing.feature_groups.data_operations.row_preserving.rank", "mloda.testing.feature_groups.data_operations.row_preserving.scalar_aggregate", "mloda.testing.feature_groups.data_operations.row_preserving.window_aggregation", "mloda.testing.feature_groups.data_operations.string"]
