# AUTO-GENERATED by scripts/generate_pyproject.py
# Do not edit directly - modify config/shared.toml or config/packages.toml instead

[build-system]
requires = ["setuptools>=77.0.1"]
build-backend = "setuptools.build_meta"

[project]
name = "mloda-community-openlineage"
version = "0.4.6"
description = "OpenLineage RunEvents with schema, data-source and parent-run facets, for mloda pipelines"
license = "Apache-2.0"
authors = [{ name = "Tom Kaltofen", email = "info@mloda.ai" }]
dependencies = ["mloda>=0.12.0,<0.13.0", "openlineage-python>=1.20,<2"]
requires-python = ">=3.10,<3.15"

[project.optional-dependencies]
dev = ["mloda-testing[openlineage]", "pytest>=9.0.3", "pandas", "pyarrow"]

[project.urls]
Homepage = "https://mloda.ai"
Repository = "https://github.com/mloda-ai/mloda-registry"

[project.entry-points."mloda.extenders"]
mloda-community-openlineage = "mloda.community.extenders.openlineage.manifest:EXTENDERS"

[tool.setuptools]
package-dir = {"" = "../../../.."}
packages = ["mloda.community.extenders.openlineage"]

[tool.setuptools.package-data]
"mloda.community.extenders.openlineage" = ["py.typed"]
