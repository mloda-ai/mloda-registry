# AUTO-GENERATED by scripts/generate_pyproject.py
# Do not edit directly - modify config/shared.toml or config/packages.toml instead

[build-system]
requires = ["setuptools>=77.0.1"]
build-backend = "setuptools.build_meta"

[project]
name = "mloda-community-otel"
version = "0.4.5"
description = "OpenTelemetry spans, metadata-only by default, for mloda pipelines"
license = "Apache-2.0"
authors = [{ name = "Tom Kaltofen", email = "info@mloda.ai" }]
dependencies = ["mloda>=0.11.2,<0.12.0", "opentelemetry-api>=1.30,<2"]
requires-python = ">=3.10,<3.15"

[project.optional-dependencies]
dev = ["mloda-testing", "pytest>=9.0.3", "opentelemetry-sdk>=1.30,<2"]

[project.urls]
Homepage = "https://mloda.ai"
Repository = "https://github.com/mloda-ai/mloda-registry"

[project.entry-points."mloda.extenders"]
mloda-community-otel = "mloda.community.extenders.otel.manifest:EXTENDERS"

[tool.setuptools]
package-dir = {"" = "../../../.."}
packages = ["mloda.community.extenders.otel"]

[tool.setuptools.package-data]
"mloda.community.extenders.otel" = ["py.typed"]
