# AUTO-GENERATED by scripts/generate_pyproject.py
# Do not edit directly - modify config/shared.toml or config/packages.toml instead

[build-system]
requires = ["setuptools>=77.0.1"]
build-backend = "setuptools.build_meta"

[project]
name = "mloda-community-binary-model"
version = "0.4.5"
description = "Mixin running an external binary as a model over Arrow IPC, the base for binary-backed FeatureGroups"
license = "Apache-2.0"
authors = [{ name = "Tom Kaltofen", email = "info@mloda.ai" }]
dependencies = ["mloda>=0.11.1,<0.12.0"]
requires-python = ">=3.10,<3.15"

[project.optional-dependencies]
dev = ["mloda-testing[binary-model]", "pytest>=9.0.3"]
pyarrow = ["pyarrow>=25"]

[project.urls]
Homepage = "https://mloda.ai"
Repository = "https://github.com/mloda-ai/mloda-registry"

[tool.setuptools]
package-dir = {"" = "../../../.."}
packages = ["mloda.community.feature_groups.binary_model"]
