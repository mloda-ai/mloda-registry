# AUTO-GENERATED by scripts/generate_pyproject.py
# Do not edit directly - modify config/shared.toml or config/packages.toml instead

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mloda-community-data-operations"
version = "0.2.12"
description = "Shared base classes for data operation feature groups"
license = "Apache-2.0"
authors = [{ name = "Tom Kaltofen", email = "info@mloda.ai" }]
dependencies = ["mloda>=0.5.5"]
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["mloda-testing", "pytest"]
all = ["mloda-community-window-aggregation", "mloda-community-group-aggregation"]

[project.urls]
Homepage = "https://mloda.ai"
Repository = "https://github.com/mloda-ai/mloda-registry"

[tool.setuptools]
package-dir = {"" = "../../../.."}
packages = ["mloda.community.feature_groups.data_operations", "mloda.community.feature_groups.data_operations.row_preserving"]
