# AUTO-GENERATED by scripts/generate_pyproject.py
# Do not edit directly - modify config/shared.toml or config/packages.toml instead

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mloda-community-group-aggregation"
version = "0.2.12"
description = "Group aggregation feature group (reduce, partition_by, one row per group)"
license = "Apache-2.0"
authors = [{ name = "Tom Kaltofen", email = "info@mloda.ai" }]
dependencies = ["mloda-community-data-operations>=0.2.12"]
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["mloda-testing", "pytest"]
pyarrow = ["pyarrow"]
sqlite = []
duckdb = ["duckdb"]
polars = ["polars"]
pandas = ["pandas"]
all = ["pyarrow", "polars", "pandas", "duckdb"]

[project.urls]
Homepage = "https://mloda.ai"
Repository = "https://github.com/mloda-ai/mloda-registry"

[tool.setuptools]
package-dir = {"" = "../../../../.."}
packages = ["mloda.community.feature_groups.data_operations.group_aggregation"]
