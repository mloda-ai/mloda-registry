# AUTO-GENERATED by scripts/generate_pyproject.py
# Do not edit directly - modify config/shared.toml or config/packages.toml instead

[build-system]
requires = ["setuptools>=77.0.1"]
build-backend = "setuptools.build_meta"

[project]
name = "mloda-enterprise-audit"
version = "0.4.7"
description = "Tenant-scoped audit records with an identity presence gate for mloda pipelines"
license = "LicenseRef-Proprietary"
authors = [{ name = "Tom Kaltofen", email = "info@mloda.ai" }]
dependencies = ["mloda>=0.13.0,<0.14.0"]
requires-python = ">=3.10,<3.15"

[project.optional-dependencies]
dev = ["mloda-testing", "pytest>=9.0.3"]

[project.urls]
Homepage = "https://mloda.ai"
Repository = "https://github.com/mloda-ai/mloda-registry"

[project.entry-points."mloda.extenders"]
mloda-enterprise-audit = "mloda.enterprise.extenders.audit.manifest:EXTENDERS"

[tool.setuptools]
package-dir = {"" = "../../../.."}
packages = ["mloda.enterprise.extenders.audit"]
